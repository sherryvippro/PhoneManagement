@model IEnumerable<Admin.Models.THoaDonBan>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NgayBan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TongHdb)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaKhNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NgayBan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TongHdb)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaKhNavigation.MaKh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SoHdb">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SoHdb">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SoHdb">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
